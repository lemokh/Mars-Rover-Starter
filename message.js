const command = require("./command");
// Message object has a name & contains several Command objects
// Message bundles & delivers commands to rover
class Message {}

module.exports = Message;

//  first write unit tests for each feature,
//  then write the class to pass each test
